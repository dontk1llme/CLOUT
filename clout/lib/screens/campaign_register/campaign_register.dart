import 'package:flutter/material.dart';

class CampagignRegister extends StatelessWidget {
  const CampagignRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}