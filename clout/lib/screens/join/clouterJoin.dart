import 'package:flutter/material.dart';

class ClouterJoin extends StatelessWidget {
  const ClouterJoin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Text('클라우터 조인'),
            // 다른 위젯들을 추가하거나 정의하십시오.
          ],
        ),
      ),
    );
  }
}
